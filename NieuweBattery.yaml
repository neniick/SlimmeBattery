blueprint:
  name: "Nul op de Meter + Target SOC Sunset (25kWh Edition)"
  description: >
    Berekent het tekort aan energie om 75% SOC te halen bij zonsondergang, 
    rekening houdend met de solar forecast, en vult dit aan tijdens de goedkoopste uren.
  domain: automation

  input:
    update_interval:
      name: "Update frequentie"
      default: "/20"
      selector:
        select:
          options: [{label: "10s", value: "/10"}, {label: "20s", value: "/20"}, {label: "30s", value: "/30"}]

    # ---- Sensors ----
    sensor_soc:
      name: "Batterij SOC (%)"
      selector: { entity: { domain: sensor } }
    sensor_house_power:
      name: "Netverbruik (W)"
      selector: { entity: { domain: sensor } }
    sensor_price_value:
      name: "Zonneplan Prijs (€/kWh)"
      selector: { entity: { domain: sensor } }
    sensor_price_level:
      name: "Zonneplan Prijsniveau (Status)"
      selector: { entity: { domain: sensor } }
    sensor_solar_forecast_remaining:
      name: "Forecast.Solar - Resterende productie vandaag (kWh)"
      description: "Gebruik de 'remaining' sensor voor de beste nauwkeurigheid."
      selector: { entity: { domain: sensor } }

    # ---- Parameters ----
    battery_capacity_kwh:
      name: "Batterij Capaciteit (kWh)"
      default: 25
      selector: { number: { min: 2, max: 100, step: 0.5, unit_of_measurement: "kWh" } }

    target_soc_at_sunset:
      name: "Doel SOC bij zonsondergang (%)"
      default: 75
      selector: { number: { min: 0, max: 100, step: 5, unit_of_measurement: "%" } }

    max_charge_price:
      name: "Maximale prijs voor laden uit net (€)"
      default: 0.25
      selector: { number: { min: 0, max: 1, step: 0.01, unit_of_measurement: "€" } }

    # ---- Actuators ----
    select_force_mode:
      name: "Solis Force Mode Selector"
      selector: { entity: { domain: select } }
    number_charge_power:
      name: "Max Charge Power Actuator"
      selector: { entity: { domain: number } }
    number_discharge_power:
      name: "Max Discharge Power Actuator"
      selector: { entity: { domain: number } }

    # ---- Nul op de meter basis ----
    grid_deadband_w:
      name: "Deadband (W)"
      default: 300
      selector: { number: { min: 50, max: 1000, step: 50 } }
    soc_hard_min:
      name: "Harde SOC Ondergrens voor huisverbruik (%)"
      default: 15
      selector: { number: { min: 5, max: 30 } }

variables:
  v_soc_ent: !input sensor_soc
  v_house_pwr: !input sensor_house_power
  v_price_ent: !input sensor_price_value
  v_price_lvl_ent: !input sensor_price_level
  v_forecast_ent: !input sensor_solar_forecast_remaining
  v_cap: !input battery_capacity_kwh
  v_target_soc: !input target_soc_at_sunset
  v_max_p: !input max_charge_price
  v_deadband: !input grid_deadband_w
  v_soc_hard_min: !input soc_hard_min
  v_prefix: "sensor.zonneplan_forcast_tariff_"

trigger:
  - platform: time_pattern
    seconds: !input update_interval

action:
  - variables:
      soc: "{{ states(v_soc_ent) | float(0) }}"
      grid_w: "{{ states(v_house_pwr) | float(0) }}"
      price: "{{ states(v_price_ent) | float(99) }}"
      price_lvl: "{{ states(v_price_lvl_ent) | lower }}"
      solar_remaining: "{{ states(v_forecast_ent) | float(0) }}"
      
      # Hoeveel kWh willen we minimaal in de accu hebben (75%)
      target_kwh: "{{ (v_target_soc / 100) * v_cap }}"
      # Hoeveel kWh zit er nu in?
      current_kwh: "{{ (soc / 100) * v_cap }}"
      
      # Het tekort dat we met het net MOETEN vullen
      # Tekort = Doel - (Huidig + Wat er nog van de zon komt)
      net_charge_needed_kwh: "{{ (target_kwh - (current_kwh + solar_remaining)) | max(0) }}"

      # Forecast analyse (Check of dit het goedkoopste uur is)
      price_analysis: >-
        {% set ns = namespace(p=[]) %}
        {% for i in range(1, 9) %}
          {% set s = states(v_prefix ~ i) %}
          {% if s not in ['unknown','unavailable',''] %}{% set ns.p = ns.p + [s|float] %}{% endif %}
        {% endfor %}
        {% set min_f = ns.p | min if ns.p else price %}
        {% set max_f = ns.p | max if ns.p else price %}
        {{ 'true' if price <= min_f else 'false' }},{{ 'true' if price >= max_f else 'false' }}

      is_lowest: "{{ price_analysis.split(',')[0] == 'true' }}"
      is_highest: "{{ price_analysis.split(',')[1] == 'true' }}"

      # BESLISSING: Laden we nu uit het net?
      # Condities: Prijs=low, Prijs < Cap, We hebben een tekort (> 0.5 kWh), en het is het goedkoopste uur.
      should_grid_charge: >-
        {{ price_lvl == 'low' and price <= v_max_p and net_charge_needed_kwh > 0.5 and is_lowest }}

      # BESLISSING: Verkopen bij hoge prijs? (Alleen als we boven 40% zitten om huisverbruik te sparen)
      should_sell: "{{ price_lvl == 'high' and is_highest and soc > 40 }}"

  - variables:
      cur_m: "{{ states(!input select_force_mode) }}"
      cur_c: "{{ states(!input number_charge_power) | float(0) if cur_m == 'Solis RC Force Battery Charge' else 0 }}"
      cur_d: "{{ states(!input number_discharge_power) | float(0) if cur_m == 'Solis RC Force Battery Discharge' else 0 }}"
      real_demand: "{{ grid_w + cur_d - cur_c }}"

      target_mode: >-
        {% if should_grid_charge %}{{ 'Charge' }}
        {% elif should_sell %}{{ 'Discharge' }}
        {% elif real_demand > v_deadband and soc > v_soc_hard_min %}{{ 'Discharge' }}
        {% elif real_demand < (0 - v_deadband) and soc < 100 %}{{ 'Charge' }}
        {% else %}{{ 'None' }}{% endif %}

      target_power: >-
        {% if should_grid_charge %}{{ 4000 }}
        {% elif should_sell %}{{ 4000 }}
        {% elif target_mode == 'Discharge' %}{{ [real_demand | abs | int, 6000] | min }}
        {% elif target_mode == 'Charge' %}{{ [real_demand | abs | int, 8000] | min }}
        {% else %}{{ 0 }}{% endif %}

  # UITVOERING
  - choose:
      - conditions: "{{ target_mode == 'Charge' }}"
        sequence:
          - service: number.set_value
            target: { entity_id: !input number_charge_power }
            data: { value: "{{ target_power }}" }
          - service: select.select_option
            target: { entity_id: !input select_force_mode }
            data: { option: "Solis RC Force Battery Charge" }
      
      - conditions: "{{ target_mode == 'Discharge' }}"
        sequence:
          - service: number.set_value
            target: { entity_id: !input number_discharge_power }
            data: { value: "{{ target_power }}" }
          - service: select.select_option
            target: { entity_id: !input select_force_mode }
            data: { option: "Solis RC Force Battery Discharge" }
            
    default:
      - service: select.select_option
        target: { entity_id: !input select_force_mode }
        data: { option: "None" }

mode: single
